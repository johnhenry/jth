// Fibonacci sequence using the built-in fibonacci operator
//
// fibonacci pops (a, b) and pushes (b, a, a+b) — three values.
// We use swap drop to trim back to two values [b, a+b] for the next step.
// @ peeks (logs the top without removing it).

0 1;

// Print 15 fibonacci numbers
#[
  fibonacci     // [a, b] => [b, a, a+b]
  swap drop     // [b, a, a+b] => [b, a+b]
  @             // log a+b (peek, no removal)
] 15 times;

drop drop;      // clean up
