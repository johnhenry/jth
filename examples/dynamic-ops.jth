// Dynamic operators
//
// Prefix a number to an operator to create a partial application.
// The semantics are: N op value, i.e. the prefix number comes first.
//
// For commutative operations (+, *) the order doesn't matter.
// For non-commutative operations (-, /, %, **):
//   "3-" means (3 - x), "2/" means (2 / x), "3**" means (3 ** x)

// Addition (commutative — N + x)
10 3+ peek;     // 13
10 5+ peek;     // 15

// Subtraction (N - x)
20 3- peek;     // 3 - 20 = -17

// Multiplication (commutative — N * x)
7 2* peek;      // 14
5 10* peek;     // 50

// Division (N / x)
50 100/ peek;   // 100 / 50 = 2

// Modulo (N % x)
17 5% peek;     // 5 % 17 = 5

// Exponentiation (N ** x)
3 2** peek;     // 2 ** 3 = 8
2 3** peek;     // 3 ** 2 = 9

// Logarithm (log base N of x)
100 10log peek;   // 2  (log base 10 of 100)
8 2log peek;      // 3  (log base 2 of 8)

// Dynamic operators are useful in pipelines
10;
3+ peek;     // 13
2* peek;     // 26
drop;
